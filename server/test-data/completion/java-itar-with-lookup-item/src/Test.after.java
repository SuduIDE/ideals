class Test {
    void test(Integer[] items) {
        for (int ${1:i} = 0; ${1:i} < ${2:items}.length; ${1:i}++) {
            ${3:Integer} ${4:item} = ${2:items}[${1:i}];
            ${0:}
        }
    }
}